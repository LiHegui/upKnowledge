# 笔试系列

> 融合「手撕代码系列」与「算法 Code」，作为统一笔试复习入口。

---

## 手撕实现题

- 订阅发布
- 防抖、节流
- 观察者模式
- 实现一个 bind
- 实现 new 关键字
- 实现一个 redux
- 实现 Vue 响应式
- 实现一个 Promise
- 实现一个 Promise.all
- 实现一个快排
- 实现一个深拷贝

### 防抖、节流

```js
function fn1(callback, delay) {
	let lastTime = 0;
	return function () {
		const now = Date.now();
		if (now - lastTime > delay) {
			callback.apply(this, arguments);
			lastTime = now;
		}
	};
}

function fn2(callback, delay) {
	let timer = null;
	return function () {
		clearTimeout(timer);
		timer = setTimeout(() => {
			callback.apply(this, arguments);
		}, delay);
	};
}
```

### 实现一个 bind

```js
Function.prototype.myBind = function (context) {
	if (typeof this !== 'function') {
		throw new TypeError('Error');
	}
	const args = [...arguments].slice(1);
	const fn = this;
	return function Fn() {
		return fn.apply(
			this instanceof Fn ? new fn(...arguments) : context,
			args.concat(...arguments)
		);
	};
};
```

### 实现 new 关键字

```js
function myNew(Func, ...args) {
	const obj = Object.create(Func.prototype);
	const result = Func.apply(obj, args);
	return result instanceof Object ? result : obj;
}
```

### 实现 Promise.all

```js
function promiseAll(promises) {
	return new Promise((resolve, reject) => {
		const results = [];
		let count = 0;
		for (let i = 0; i < promises.length; i++) {
			promises[i]
				.then((result) => {
					results[i] = result;
					count++;
					if (count === promises.length) {
						resolve(results);
					}
				})
				.catch((error) => reject(error));
		}
	});
}
```

---

## 算法必会题

- 如何实现一个栈？
- 二分查找
- 排序（冒泡、快排）
- 递归转非递归（栈模拟）
- 斐波那契数列
- 字符串混合排序（字母+数字）

### 如何实现一个栈

```js
class Stack {
	constructor() {
		this.items = [];
	}

	push(item) {
		this.items.push(item);
	}

	pop() {
		return this.items.pop();
	}

	peek() {
		return this.items[this.items.length - 1];
	}

	isEmpty() {
		return this.items.length === 0;
	}
}
```

### 二分查找

```js
function binarySearch(arr, s, e, target) {
	if (s > e) return -1;
	const mid = Math.floor((s + e) / 2);
	const midValue = arr[mid];

	if (midValue === target) return mid;
	if (midValue < target) {
		return binarySearch(arr, mid + 1, e, target);
	}
	return binarySearch(arr, s, mid - 1, target);
}
```

### 快速排序

```js
function bubbleSort(arr) {
	for (let i = 0; i < arr.length; i++) {
		for (let j = i + 1; j < arr.length; j++) {
			if (arr[i] > arr[j]) {
				const temp = arr[i];
				arr[i] = arr[j];
				arr[j] = temp;
			}
		}
	}
	return arr;
}

function quickSort(arr, s, e) {
	if (s >= e) return;
	const basicValue = arr[s];
	let swapIndex = s;

	for (let i = s + 1; i <= e; i++) {
		if (arr[i] < basicValue) {
			swapIndex++;
			const temp = arr[i];
			arr[i] = arr[swapIndex];
			arr[swapIndex] = temp;
		}
	}

	const temp = arr[swapIndex];
	arr[swapIndex] = arr[s];
	arr[s] = temp;

	quickSort(arr, s, swapIndex - 1);
	quickSort(arr, swapIndex + 1, e);
}
```

### 递归转非递归

递归转非递归通常使用栈数据结构来模拟调用栈。

### 斐波那契数列

```js
function fun(n) {
	if (n <= 1) return n;
	return fun(n - 1) + fun(n - 2);
}

function fibonacci(n) {
	const stack = [];
	stack.push(n);
	let result = 0;
	while (stack.length > 0) {
		const current = stack.pop();
		if (current <= 1) {
			result += current;
		} else {
			stack.push(current - 1);
			stack.push(current - 2);
		}
	}
	return result;
}
```

### 字符串混合排序

```js
function sortMixedData(data) {
	return data.sort((a, b) => {
		const numA = Number(a.slice(1));
		const numB = Number(b.slice(1));

		if (numA === numB) {
			const typeA = a.charCodeAt(0);
			const typeB = b.charCodeAt(0);
			return typeA - typeB;
		}

		return numA - numB;
	});
}
```

---

## 建议刷题顺序

1. 手撕实现题（高频 API 与设计模式）
2. 排序与栈
3. 递归转非递归
4. 二分查找与斐波那契复杂度分析
