// 无重复字符的最长子串
/**
 * @param {string} s
 * @return {number}
 */
var lengthOfLongestSubstring = function (s) {
    let _arr = s.split('')
    let max = 0
    for (let i = 0; i < _arr.length; i++) {
        let ac = _arr[i], move = _arr[i]
        
    }
    return max
};
console.log(lengthOfLongestSubstring("pwwkew"));
